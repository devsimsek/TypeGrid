### **Task List**

- [ ] **1. Create Album (`c`)**
  - Add a keybinding in the Album List.
  - Prompt the user for an album title and folder name.
  - Create the physical folder in `/images/` and a new project entry in `typegrid.json`.
- [x] **2. Edit Metadata (`e`)**
  - Add a keybinding in the Image List.
  - Open a prompt sequence to edit `camera`, `lens`, and `tags` for the currently selected image.
  - Save changes directly to `typegrid.json`.
- [x] **3. Set Primary Image (`p`)**
  - Add a keybinding to mark the selected image as `primary: true` (and automatically set all other images in the album to `primary: false`).
- [x] **4. Open Image in OS (`o`)**
  - Add a keybinding to open the image file.
  - Implement a cross-platform helper using `child_process` (`open` for macOS, `start` for Windows, `xdg-open` for Linux) to launch the default image viewer.
- [x] **5. Autoscan Album (`s`)**
  - Add a keybinding to scan the physical `/images/<album>` directory.
  - Compare files on disk against the images mapped in `typegrid.json`.
  - For each unmapped file, show a prompt asking: "Found new image `xyz.jpg`. Add to album? (y/n)".
- [x] **6. OTA Update Logic (`u`)**
  - Add a keybinding (or CLI argument) to check the latest version.
  - Fetch `package.json` from `https://raw.githubusercontent.com/devsimsek/TypeGrid/main/package.json` to compare versions.
  - If a newer version exists, fetch the `CHANGELOG.md` and display the latest release notes.
  - Prompt the user to update. If yes, pull the latest CLI scripts, CSS themes, and JS files directly from GitHub.
- [x] **7. Granular Edits - [ ] **7. Granular Edits & Album Management** Album Management**
  - Edit Album metadata (Title, Year, Description, Tags) when pressing `e` in Albums list.
  - Delete Album when pressing `d` in Albums list.
  - Quick-edit Image Tags (`t`), Camera (`c`), and Lens (`l`) individually.
- [x] **8. Help - [ ] **8. Help & Version Info** Version Info**
  - Add a help menu or modal (`?`) to display all available keybindings.
  - Display current TypeGrid version in the UI (e.g., bottom bar or in the help menu).